# testapp
A test app created via Terraform

## display_tour_price_map

Utility function to render an interactive map with the tour price displayed
directly at a given location. Example:

```python
from tour_map import display_tour_price_map

display_tour_price_map("El Nido, Palawan")
```
